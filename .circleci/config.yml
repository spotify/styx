version: 2.1
jobs:
  no-op:
    docker:
      - image: non-existing-image
    steps:
      - run: echo "No-op job"
workflows:
  version: 2
  build:
    jobs:
      - no-op:
          filters:
            branches:
              ignore: gh-pages
