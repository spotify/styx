version: 2.1
jobs:
  no-op:
    docker:
      - image: circleci/python:3.9
    steps:
      - run: echo "No-op job"
workflows:
  version: 2
  build:
    jobs:
      - no-op:
          filters:
            branches:
              ignore: gh-pages
